language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t sazzadrupak/frontend-aws-docker - Dockerfile.dev .

script:
  - docker run -e CI=true sazzadrupak/frontend-aws-docker npm run test
